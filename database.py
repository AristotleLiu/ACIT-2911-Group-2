from flask_sqlalchemy import SQLAlchemy

# Create an instance of the SQLAlchemy class
db = SQLAlchemy()

"""
The db variable is an instance of the SQLAlchemy class, which is a toolkit
for working with databases in Flask applications. It provides an interface to
interact with the database and perform common database operations, such as
querying, inserting, updating, and deleting records.
"""
